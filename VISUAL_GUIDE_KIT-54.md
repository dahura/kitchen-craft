# KIT-54: Visual Guide - Анимация Чата

## 🎨 Архитектура компонентов

```
ChatPanel (Parent)
├── Activity (Управляет видимостью)
│   └── Chat Component
│       ├── Header (Всегда видим)
│       ├── Activity → Messages (Скрыто/Видимо)
│       └── Activity → Input Area (Скрыто/Видимо)
```

---

## 📊 Таблица состояний

```
┌─────────────────┬─────────────┬───────────────┬─────────────────┐
│   Состояние     │  isCollapsed │ Activity Mode │    Результат    │
├─────────────────┼─────────────┼───────────────┼─────────────────┤
│  Развернуто     │     false   │   "visible"   │ Полностью видим │
│  Свертывается   │     true    │   "hidden"    │ Анимация 500ms  │
│  Свернуто       │     true    │   "hidden"    │ Только header   │
│  Разворачивается│     false   │   "visible"   │ Анимация 500ms  │
└─────────────────┴─────────────┴───────────────┴─────────────────┘
```

---

## 🎬 Временная шкала анимации

### Развертывание (Expand)
```
TIMELINE: 0ms ───────── 500ms ───────► 
          
Click ──→ 
         ↓
isCollapsed: false
         ↓
CSS Classes: scale-y-100 opacity-100
         ↓
         ├─ scale-y: 0 → 1 (ease-in-out)
         ├─ opacity: 0 → 1 (ease-in-out)
         └─ slideInUp: translateY(10px) → 0
         ↓
         ✅ Чат готов к использованию
```

### Сворачивание (Collapse)
```
TIMELINE: 0ms ───────── 500ms ────────────── 1000ms ───► 
          
Auto Close ──→ 
            ↓
isCollapsed: true
            ↓
CSS Classes: scale-y-0 opacity-0
            ↓
            ├─ scale-y: 1 → 0 (ease-in-out)
            ├─ opacity: 1 → 0 (ease-in-out)
            └─ slideOutDown: translateY(0) → 10px
            ↓
         500ms: CSS анимация завершена
            ↓
         Activity скрывает элементы
         ├─ visibility: hidden
         └─ pointer-events: none
            ↓
            ✅ Видна только header
```

---

## 📐 CSS Трансформации

### Expand State
```
┌─────────────────────────────┐
│  HEADER (Всегда видим)      │
├─────────────────────────────┤
│                             │
│   MESSAGES (Видимы)         │ ← opacity: 1
│   ├─ Message 1              │   scale-y: 1
│   ├─ Message 2              │   translateY: 0
│   └─ Message 3              │
│                             │
├─────────────────────────────┤
│   INPUT AREA (Видима)       │ ← opacity: 1
│   ├─ Input Field            │   scale-y: 1
│   └─ Send Button            │   translateY: 0
└─────────────────────────────┘
```

### Collapse State
```
┌─────────────────────────────┐
│  HEADER (Видим)             │ ← opacity: 1
├─────────────────────────────┤
│                             │
│   MESSAGES (Скрыты)         │ ← opacity: 0
│   (visibility: hidden)      │   scale-y: 0
│                             │   translateY: 10px
│                             │
├─────────────────────────────┤
│   INPUT AREA (Скрыта)       │ ← opacity: 0
│   (pointer-events: none)    │   scale-y: 0
│                             │   translateY: 10px
└─────────────────────────────┘
```

---

## 🔄 State Machine

```
                   ┌──────────────────────┐
                   │   COLLAPSED STATE    │
                   │  ✓ Only Header       │
                   │  ✓ Hidden content    │
                   └──────────────────────┘
                            ▲
                            │ After 3 sec blur
                    ┌───────┴───────┐
                    │   Collapse    │
                    │   Animation   │ (500ms)
                    │   500ms       │
                    └───────┬───────┘
                            │
                   ┌────────▼────────────┐
                   │  EXPANDED STATE     │
                   │  ✓ Full content     │
                   │  ✓ Messages visible │
                   │  ✓ Input ready      │
                   └────────┬────────────┘
                            ▲
                            │ On input click
                    ┌───────┴───────┐
                    │   Expand      │
                    │   Animation   │ (500ms)
                    │   500ms       │
                    └───────┬───────┘
                            │
                   [Click/Focus detected]
```

---

## 📈 Performance Characteristics

### Animation Metrics
```
┌──────────────────┬──────────┐
│     Property     │  Value   │
├──────────────────┼──────────┤
│ Duration         │  500ms   │
│ Easing          │ ease-in-out |
│ GPU Accelerated  │  ✅ Yes  │
│ Jank Risk       │  ⚠️ Low   │
│ Memory Impact    │  🟢 Good |
│ Paint Operations │  Minimal │
└──────────────────┴──────────┘
```

### Layers
```
GPU Rendering Layers:
├─ Transform Layer (scale-y, translateY)
├─ Opacity Layer (opacity)
└─ Composite Layer (z-index management)

No Layout Recalculations Needed ✅
```

---

## 🎯 User Flow

```
User Interface Flow:

START
  │
  ├─► SEES HEADER
  │   └─► Collapsed Chat
  │       └─► Header visible only
  │
  ├─► CLICKS INPUT
  │   ├─► isCollapsed = false
  │   └─► Animation: Expand (500ms)
  │       ├─ scale-y: 0 → 1
  │       ├─ opacity: 0 → 1
  │       └─ slideInUp
  │       └─► SEES EXPANDED CHAT
  │           ├─ Messages visible
  │           ├─ Input ready
  │           └─ Cursor in input
  │
  ├─► TYPES & WAITS
  │   ├─► 3 seconds pass
  │   ├─► Input loses focus (blur)
  │   ├─► Auto-collapse triggered
  │   └─► Animation: Collapse (500ms)
  │       ├─ scale-y: 1 → 0
  │       ├─ opacity: 1 → 0
  │       └─ slideOutDown
  │       └─► BACK TO HEADER ONLY
  │
  └─► REPEAT

Close/Toggle Button:
  Any time: CLICK button
  └─► toggles isCollapsed directly
```

---

## 🛠 Component API

### Activity Component

```typescript
interface ActivityProps {
  children: ReactNode;
  mode: "visible" | "hidden" | "skip";
}

// Usage:
<Activity mode={isCollapsed ? "hidden" : "visible"}>
  <div>Content</div>
</Activity>
```

**Режимы:**
- `"visible"` → Элемент видим, pointer-events: auto
- `"hidden"` → Элемент скрыт, но в DOM для анимаций
- `"skip"` → Не управляет элементом

---

## 📚 Файлы и их роли

```
components/activity.tsx
├─ Экспортирует Activity компонент
├─ Управляет visibility с задержкой
└─ Поддерживает ARIA атрибуты

chat-panel.tsx
├─ Управляет isCollapsed состоянием
├─ Оборачивает Chat в Activity
└─ Обрабатывает focus/blur события

chat.tsx
├─ Содержит UI Chat
├─ Оборачивает Messages в Activity
├─ Оборачивает Input в Activity
└─ Показывает Header всегда

globals.css
├─ Определяет slideInUp анимацию
├─ Определяет slideOutDown анимацию
└─ Связывает с [data-activity-mode]
```

---

## ✨ Улучшения

### ДО (Проблема)
```
Клик input
  ↓
isCollapsed = false
  ↓
{!isCollapsed && <Chat />} → мгновенно добавляет
  ↓
CSS переход НЕ срабатывает (элемент уже добавлен)
  ↓
❌ Чат мгновенно появляется
```

### ПОСЛЕ (Решение)
```
Клик input
  ↓
isCollapsed = false
  ↓
Activity mode → "visible"
  ↓
CSS переход СРАБАТЫВАЕТ (элемент уже в DOM)
  ↓
500ms плавная анимация
  ↓
✅ Чат красиво разворачивается
```

---

## 🎓 Ключевые Концепции

### 1. Activity Полифилл
Компонент, который имитирует будущий React API для управления видимостью без размонтирования.

### 2. DOM Preservation
Элементы остаются в DOM при скрытии, позволяя CSS анимациям завершиться.

### 3. Timing
500ms задержка совпадает с Tailwind `duration-500` для синхронизированной анимации.

### 4. GPU Acceleration
Использование `transform` и `opacity` для GPU-ускоренных анимаций.

---

**Визуальная документация завершена** ✅

